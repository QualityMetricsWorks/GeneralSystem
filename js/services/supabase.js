import{ENV}from"../config/env.js";import{createClient}from"https://esm.sh/@supabase/supabase-js@2";export const configured=!ENV.SUPABASE_URL.includes("YOUR_PROJECT")&&!ENV.SUPABASE_ANON_KEY.includes("YOUR_SUPABASE");export const supabase=configured?createClient(ENV.SUPABASE_URL,ENV.SUPABASE_ANON_KEY,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:false}}):null;
